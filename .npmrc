# Use legacy-peer-deps to avoid peer dependency conflicts
legacy-peer-deps=true

# Include optional dependencies (npm will automatically install only platform-appropriate ones)
# This ensures Linux bindings (@oxc-parser/binding-linux-x64-gnu) are installed
# while Windows bindings (@oxc-parser/binding-win32-*) are skipped on Linux

