# Use legacy-peer-deps to avoid peer dependency conflicts
legacy-peer-deps=true

# Note: Optional dependencies are handled via --omit=optional flag in nixpacks.toml
# This prevents Windows-specific packages (@oxc-parser/binding-win32-*) from being installed on Linux

