# NPM Configuration for cPanel Deployment
# This file is used during npm install to prevent platform mismatch errors

# Enable hoisting for cPanel compatibility
shamefully-hoist=true

# Skip optional dependencies to avoid platform mismatch errors
# Note: IPX provider requires Sharp (regular dependency, not optional)
# If Sharp install fails, rebuild it on server: npm rebuild sharp
optional=false

